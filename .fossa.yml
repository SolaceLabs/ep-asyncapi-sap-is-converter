version: 3

project:
  locator: SolaceLabs_ep-asyncapi-sap-is-converter
  id: SolaceLabs_ep-asyncapi-sap-is-converter
  name: ep-asyncapi-sap-is-converter
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
